# --------------------------------------------------------------------------- #
# Core agent dependencies (lightweight, CPU-friendly)
# --------------------------------------------------------------------------- #
ollama>=0.3.0            # Python client for the local Ollama server
requests>=2.31.0         # HTTP for SearXNG + Stable Diffusion APIs
beautifulsoup4>=4.12.0   # HTML parsing for fetched search result pages
Pillow>=10.0.0           # image loading / mask handling

# --------------------------------------------------------------------------- #
# Human-parsing mask generation (generate_mask.py)
# --------------------------------------------------------------------------- #
# These pull in PyTorch. On the M1200 you can install the CPU build of torch to
# keep the GPU free for Stable Diffusion (see README). transformers ships the
# SegFormer clothes-parsing model used to build precise clothing masks.
transformers>=4.40.0
torch>=2.0.0
numpy>=1.24.0

# --------------------------------------------------------------------------- #
# Offline document RAG (search_tools.py, offline mode)
# --------------------------------------------------------------------------- #
# pypdf is optional but enables reading PDFs you drop into offline_docs/.
pypdf>=4.0.0
